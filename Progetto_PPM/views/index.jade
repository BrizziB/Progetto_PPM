extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/registrazione") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/logout") Logout